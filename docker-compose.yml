services:
# ---- PostgreSQL Database ----
  db:
    image: postgres:16-alpine
    container_name: jerney-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: jerney-user
      POSTGRES_PASSWORD: jerney-pass-2026
      POSTGRES_DB: jerney_db
    ports:
      - "5432:5432"
    volumes:
      - pg-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U jerney-user -d jerney_db"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
# Security: read-only root filesystem where possible
    tmpfs:
      - /tmp
      - /run

# ----------- backend service -----------
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: jerney-backend
    restart: unless-stopped
    expose: 
      - "5000"
    depends_on:
      db:
        condition: service_healthy
    environment:
      - DB_HOST=db
      - DB_PORT=5432
      - DB_USER=jerney-user
      - DB_PASSWORD=jerney-pass-2026
      - DB_NAME=jerney_db
# Security: do not run as root (already in Dockerfile, but enforced here too)
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp

# ----------- frontend service -----------
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: jerney-frontend
    restart: unless-stopped
    ports:
      - "80:8080"
    depends_on:
      - backend
    security_opt:
      - no-new-privileges:true

volumes:
  pg-data:
    driver: local
